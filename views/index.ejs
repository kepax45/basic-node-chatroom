<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Please choose your username</title>
    <style>
        body {
            background-color: #eeeee4;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 60vh; /* Adjust as needed */
        }
        button {
            width: 14vw;
            height: 4vh;
            font-family: 'Times New Roman', Times, serif;
            font-size: large;
            font-weight: bold;
            color:aliceblue;
            background-color: #76b5c5;
            border-style:double;
            border-color: #76b5c4;
        }
        input {
            border-radius: 1%;
            border-style: double;
            width: 30vw;
            height: 4vh
        }
    </style>
</head>
    <body>
        <input id="username"><br>
        <button id="joinbtn">JOIN</button>
        <script>
            joinButton = document.getElementById("joinbtn");
            usernameInput = document.getElementById("username");
            joinButton.onclick = () => {
                username = usernameInput.value
                if(username==="")
                    return
                window.location.replace('http://127.0.0.1/chatroom?username='+username);
            }
        </script>
    </body>
</html>